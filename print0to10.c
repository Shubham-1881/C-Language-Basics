#include<stdio.h>
int main()
{
    int x;
    for(x=0; x<=10; x = x+1)
    printf("%d \n", x);
    return 0;
}
// Syntax for For Loop is;
// for(initialisation; condition; updation)
// doThis
// return 0;
// initialisation gives initial condition
// loop will perform till it satisfy condition
// updation gives an update to a condition